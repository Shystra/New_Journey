const array = [5, 8, 10, 20, 30, 100];
const object = {}

const verifyNumber = array.forEach(( param1 ) => {
    // console.log( 'param1 =>', param1 );
    // console.log( 'param2 =>', param2 );
    // console.log( 'param2 =>', param3 );
    return param1 = param1
    
});

const changeNumber = array.map( n => n * 2)
console.log(changeNumber)

// console.log(verifyNumber)

const recebe = []

const acrescenta = recebe.push(changeNumber)

console.log(recebe)
 




const ifNumber = array.forEach((value, index) => {
    let aux = 0; 
    if (value > aux){
        aux = value;
        // console.log(aux);
    }
});



